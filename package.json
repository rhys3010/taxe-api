{
  "name": "taxe-api",
  "version": "0.2.1",
  "description": "An express.js REST API that is used to serve the Tax-E Android App.",
  "author": "Rhys Evans (rhysevans.xyz)",
  "repository": {
    "type": "git",
    "url": "https://github.com/rhys3010/taxe-api"
  },
  "scripts": {
    "start": "node ./bin/www",
    "debug": "docker-compose build && docker-compose up --build",
    "test": "docker-compose build && docker-compose -f docker-compose.test.yml up --build --abort-on-container-exit --exit-code-from app-test",
    "build": "docker-compose build"
  },
  "dependencies": {
    "basic-auth": "^1.0.4",
    "bcryptjs": "^2.3.0",
    "body-parser": "^1.15.2",
    "express": "^4.14.0",
    "jsonwebtoken": "^7.1.9",
    "mongoose": "^4.6.0",
    "mockgoose": "^8.0.1",
    "morgan": "^1.7.0",
    "nodemailer": "^2.6.0",
    "randomstring": "^1.1.5",
    "cors": "^2.8.5",
    "mocha": "^6.0.2",
    "chai": "^4.2.0",
    "chai-http": "^4.2.1"
  }
}
