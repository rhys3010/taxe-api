version: "3"
services:
  taxe:
    restart: always
    build:
      context: .
      dockerfile: Dockerfile.production
    image: taxe_api  
    ports:
      - "3000:3000"
    links:
      - mongo

  mongo:
    image: mongo
    ports:
      - "27017:27017"
    volumes:
      - ./bin/data:/data/db
